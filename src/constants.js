/*
This file contains all action types in the app. The advantage of this is two-fold;
We can more easily catch typos (as the action type will be undefined), and we
get a list of all available actions in the app, which gives a good overview!
*/

module.exports = {
	KILL_HERO: "KILL_HERO",
	RESET: "RESET"
};